<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/feed"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="edu.illinois.g31.worldview.MainActivity">

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/article1"
        android:background="@drawable/feed_background">

        <ImageView
            android:id="@+id/thumbnail"
            android:contentDescription="@string/thumbnail_description"
            android:layout_width="@dimen/thumbnail_width"
            android:layout_height="@dimen/thumbnail_height"
            app:srcCompat="@mipmap/thumbnail" />

        <TextView
            android:id="@+id/titleText"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_alignTop="@id/thumbnail"
            android:layout_toEndOf="@id/thumbnail"
            android:paddingTop="@dimen/title_vertical_padding"
            android:text="@string/title_text"
            android:textSize="@dimen/title_text_size"
            android:textColor="@color/titleColor"/>

        <TextView
            android:id="@+id/sourceText"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:paddingTop="@dimen/source_vertical_padding"
            android:paddingLeft="@dimen/source_horizontal_padding"
            android:layout_toEndOf="@id/titleText"
            android:text="@string/source_text"
            android:textSize="@dimen/source_text_size"
            android:textColor="@color/sourceColor"/>

        <TextView
            android:id="@+id/previewText"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_below="@id/titleText"
            android:layout_toEndOf="@id/thumbnail"
            android:text="@string/preview_text"
            android:textSize="@dimen/preview_text_size"
            android:textColor="@color/previewColor"/>

        <TextView
            android:id="@+id/tagText"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_alignBottom="@id/thumbnail"
            android:layout_toEndOf="@id/thumbnail"
            android:paddingBottom="@dimen/tag_vertical_padding"
            android:text="@string/tags"
            android:textSize="@dimen/tag_text_size"
            android:textColor="@color/tagColor"/>
    </RelativeLayout>


</RelativeLayout>
