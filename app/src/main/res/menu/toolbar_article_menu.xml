<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <!-- "Download", should appear as action button if possible -->
    <item
        android:id="@+id/action_download"
        android:icon="@mipmap/ic_save_black_24dp"
        app:showAsAction="ifRoom"
        android:title="@string/download_button" />

    <!-- "Add to Compare", should appear as action button if possible -->
    <item
        android:id="@+id/action_add_to_compare"
        android:icon="@mipmap/ic_add_circle_outline_black_24dp"
        app:showAsAction="ifRoom"
        android:title="@string/add_to_compare_button" />

    <!-- "Search", should appear as action button if possible -->
    <item
        android:id="@+id/action_search"
        android:icon="@mipmap/ic_search_black_24dp"
        app:showAsAction="collapseActionView|ifRoom"
        android:title="@string/search_button"
        app:actionViewClass="android.support.v7.widget.SearchView"/>

    <item
        android:id="@+id/action_sizedown"
        app:showAsAction="never"
        android:title="@string/sizedown_button" />

    <item
        android:id="@+id/action_sizeup"
        app:showAsAction="never"
        android:title="@string/sizeup_button" />


</menu>